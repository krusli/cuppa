version: '3'
services:
    users:
        build: './cuppa-users'
        ports:
            - 3000:3000
        links:
            - db:mongo
    db:
        image: mongo
        ports:
            - 27017:27017
        volumes:
            - "./db:/data/db"
